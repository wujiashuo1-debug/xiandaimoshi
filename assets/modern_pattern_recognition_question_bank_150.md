# 现代模式识别期末冲刺专项练习题库（150题）

本题库严格按照天津商业大学信息工程学院《现代模式识别》期末考试大纲及核心考点精心编制，包含**100道单选题**与**50道多选题**，全面覆盖：特征选择与提取（24分）、贝叶斯决策（36分）、线性判别（9分）、集成学习（5分）以及模式聚类（24分）五大核心板块。

---

## 一、单项选择题（共100题，每题1分）

### 模块1：模式识别概述与基础概念（第1-10题）
1. **模式识别系统的核心基本骨架流程是：**
   A. 预处理 → 数据采集 → 特征提取 → 分类/聚类判定
   B. 数据采集 → 预处理 → 特征提取 → 分类/聚类判定
   C. 特征提取 → 数据采集 → 预处理 → 分类/聚类判定
   D. 数据采集 → 特征提取 → 预处理 → 分类/聚类判定

2. **分类（Classification）与聚类（Clustering）的核心区别在于：**
   A. 分类是无监督学习，聚类是有监督学习
   B. 分类已知类别标签，聚类不知道类别标签
   C. 分类不需要特征空间，聚类需要特征空间
   D. 分类只能处理线性数据，聚类只能处理非线性数据

3. **在分类器设计中，如果模型过于复杂、包裹了训练集中的每一个点及噪声，通常会导致：**
   A. 欠拟合（Underfitting）
   B. 过拟合（Overfitting）
   C. 泛化能力增强
   D. 训练精度下降

4. **下列哪项技术不属于模式识别在计算机视觉中的生物特征识别应用？**
   A. 虹膜识别
   B. 步态识别
   C. 知识图谱实体抽取
   D. 触摸动态行为认证

5. **以特征为坐标轴构成的空间被称为：**
   A. 物理空间
   B. 样本空间
   C. 特征空间
   D. 状态空间

6. **在特征空间中，用于将不同类别样本分开的边界被称为：**
   A. 判定线/面（Decision Boundary）
   B. 坐标轴
   C. 特征向量
   D. 投影轴

7. **下列关于无监督学习的说法中，正确的是：**
   A. 必须依赖人工标注的类别标签
   B. 经典代表算法包括朴素贝叶斯和SVM
   C. 靠算法自动发现数据内部的相似性与结构
   D. 其核心任务是求解最小错误率决策面

8. **为了提高分类器的泛化能力，在设计判定边界时通常应当选择：**
   A. 极其复杂的完全包裹数据的非线性曲线
   B. 尽可能平滑且保持合理间隔的优化分类器边界
   C. 放弃任何边界，直接采用随机猜测
   D. 迫使所有类别的类内散布矩阵为零的边界

9. **模式识别通用模型 $y = f(x)$ 中，符号 $x$ 通常代表：**
   A. 系统输出的类别标签
   B. 分类器映射关系
   C. 观测数据（输入的特征向量）
   D. 先验概率分布

10. **自动驾驶感知系统中，用于获取周围环境3D点云测距的核心传感器是：**
    A. 摄像头
    B. 激光雷达（LiDAR）
    C. 毫米波雷达
    D. 超声波雷达

### 模块2：模式聚类分析（第11-35题）
11. **聚类流程的5个步骤顺序正确的是：**
    A. 选定特征 → 确定相似度 → 设定聚类准则 → 选择聚类算法 → 聚类结果评估
    B. 确定相似度 → 选定特征 → 设定聚类准则 → 选择聚类算法 → 聚类结果评估
    C. 选定特征 → 选择聚类算法 → 确定相似度 → 设定聚类准则 → 聚类结果评估
    D. 设定聚类准则 → 选定特征 → 确定相似度 → 选择聚类算法 → 聚类结果评估

12. **关于聚类分析的根本局限性，下列说法正确的是：**
    A. 聚类结果绝对唯一，不受特征选择影响
    B. 聚类具有可逆性，合并后可以完全无损拆分
    C. 聚类极易陷入局部最优，且其操作过程是不可逆的
    D. 聚类必须基于类条件概率密度才能进行

13. **在最近邻规则试探法聚类中，若圆的半径阈值 $T$ 设定过大，容易导致：**
    A. 产生大量仅包含单个样本的孤立小类
    B. 大量本不属于同类的样本被错误归为一类
    C. 算法无法收敛并死循环
    D. 聚类中心数量无限增加

14. **最近邻规则试探法最突出的不足之处是：**
    A. 计算复杂度呈指数级增长
    B. 结果极度依赖输入样本的先后顺序
    C. 无法处理在线增量学习
    D. 必须预先指定固定的类别数 $K$

15. **层次聚类算法中的融合算法（Agglomerative）采用的是什么方向的构建策略？**
    A. 自顶向下
    B. 自底向上
    C. 动态随机调整
    D. 基于密度两极划分

16. **与融合算法相比，层次聚类的分解算法（Divisive）在计算量上的特点是：**
    A. 前期计算量巨大（需要考虑所有可能的拆分组合），后期快速下降
    B. 全程计算量平稳且极小
    C. 前期计算量极小，后期呈指数级上升
    D. 计算量与样本总数完全无关

17. **K-Means（$K$均值）算法的优化核心是最小化准则函数 $E$，该函数代表：**
    A. 类间散布矩阵的特征值之和
    B. 每个样本到其所属类中心的距离平方和
    C. 各类别先验概率的熵
    D. 错分样本的加权错误率之和

18. **在实际应用中，确定K-Means算法中 $K$ 值的经典定性/定量结合方法是：**
    A. 拉格朗日乘子法
    B. 肘部法则（Look for the elbow in E-k curve）
    C. 伪逆法
    D. 留一交叉验证法

19. **当K-Means算法达到收敛状态时，准则函数 $E$ 的变化情况是：**
    A. 持续剧烈震荡
    B. 保持线性增长
    C. 不再发生变化或变化量小于给定阈值
    D. 突变为零

20. **ISODATA算法作为K-Means的升级版，其最核心的突破在于：**
    A. 能够将计算复杂度降低到 $O(1)$
    B. 可以在迭代过程中自动合并和分裂聚类中心
    C. 不需要计算任何距离度量
    D. 绝对不会陷入局部最优解

21. **在ISODATA算法中，决定两个聚类中心是否应当执行“合并”操作的直接条件是：**
    A. 这两类包含的样本数完全相等
    B. 它们之间的距离小于预设的阈值 $\theta_c$
    C. 某一类的类内标准差大于分裂阈值 $\theta_s$
    D. 迭代次数达到了最大上限

22. **ISODATA合并两个旧中心后，新聚类中心的计算方式是：**
    A. 两个旧中心的简单算术平均
    B. 随机在两个旧中心之间取一点
    C. 依据各自包含样本数进行加权平均
    D. 选取包含样本数较少的那个旧中心的值

23. **基于密度的聚类算法（如DBSCAN）的核心思想是：**
    A. 通过优化样本到类中心的几何距离来划分区域
    B. 一个点是核心点还是噪声，取决于它在 $\varepsilon$ 半径邻域内的邻居数
    C. 假设数据完全服从多元正态分布
    D. 自底向上建立一棵复杂的聚类树

24. **DBSCAN算法中，若一个点 $x_i$ 的 $\varepsilon$ 邻域内包含的样本数大于或等于 $MinPts$，则该点被定义为：**
    A. 边界点
    B. 核心点（Core Point）
    C. 噪声点（Noise Point）
    D. 孤立点

25. **相比于K-Means算法，DBSCAN算法最显著的优势是：**
    A. 需要人为指定更为复杂的 $K$ 值
    B. 对球形簇的聚类效果明显更好
    C. 能够自动识别并排除噪声点，且能发现任意形状的簇
    D. 内存消耗与样本数呈线性关系

26. **下列距离度量公式中，代表曼哈顿距离（L1距离）的是：**
    A. $d(x,y) = \sqrt{\sum (x_i - y_i)^2}$
    B. $d(x,y) = \sum |x_i - y_i|$
    C. $d(x,y) = \max |x_i - y_i|$
    D. $d(x,y) = x^T \Sigma^{-1} y$

27. **若要度量两个不同类别中心之间的离散程度，在模式识别中通常使用：**
    A. 类内散布矩阵 $S_w$
    B. 类间散布矩阵 $S_b$
    C. 总体散布矩阵 $S_t$
    D. 模糊矩阵

28. **K-Means算法对下列哪种因素非常敏感，容易导致不同的运行结果？**
    A. 样本特征的物理意义
    B. 初始聚类中心的选择
    C. 数据的存储格式
    D. 是否进行了去中心化

29. **下列关于聚类评估的说法，错误的是：**
    A. 聚类评估旨在判断分组是否合理
    B. 可以通过类内紧凑度和类间分离度进行评估
    C. 聚类评估由于没有绝对的真值标签，通常是完全主观的
    D. 好的聚类结果通常满足类内高相似、类间低相似

30. **若一个数据集包含明显的环形多层嵌套条带分布，首选的聚类算法是：**
    A. K-Means
    B. 最近邻规则试探法
    C. DBSCAN
    D. ISODATA

31. **在层次聚类中，一旦两个子类被合并，后续步骤将无法将它们重新拆分，这体现了层次聚类的：**
    A. 高效性
    B. 健壮性
    C. 不可逆性
    D. 收敛性

32. **DBSCAN算法中，低于 $MinPts$ 且不落在任何核心点邻域内的点会被标记为：**
    A. 核心点
    B. 噪声点
    C. 聚类中心
    D. 边界点

33. **在K-Means迭代过程中，当所有样本的类别分配均不再发生任何改变时，算法：**
    A. 进入死循环
    B. 达到收敛，停止迭代
    C. 触发分裂机制
    D. 必须重新初始化

34. **聚类准则设定的核心作用是：**
    A. 决定原始数据的特征如何提取
    B. 定义什么是“相似”，从而决定聚类结果的走向与停止条件
    C. 自动为每个样本分配唯一的文本标签
    D. 降低原始数据的维度

35. **在没有先验类别知识的前提下，客户细分（价值分群）应当采用哪类算法？**
    A. 支持向量机
    B. 贝叶斯分类器
    C. 聚类算法
    D. 线性判别分析

### 模块3：统计决策方法与贝叶斯分类（第36-55题）
36. **贝叶斯决策理论的核心是用概率语言回答：**
    A. 给定观测数据 $x$，它属于某一类的后验概率是多少
    B. 样本在物理空间中的几何距离是多少
    C. 弱分类器在第 $t$ 轮的错误率是多少
    D. 投影矩阵的最优特征向量是什么

37. **在贝叶斯公式 $P(\omega_i|x) = \frac{p(x|\omega_i)P(\omega_i)}{p(x)}$ 中，符合 $p(x|\omega_i)$ 表达的含义是：**
    A. 先验概率（未观测样本前，某类别占总人群的比例）
    B. 后验概率（观测到样本 $x$ 后，它属于 $\omega_i$ 类的概率）
    C. 类条件概率密度（已知类别为 $\omega_i$ 的前提下，观测到特征 $x$ 的概率密度）
    D. 特征全概率（特征空间中产生 $x$ 的总概率）

38. **若已知人群中正常人和某种疾病患者的先验比例为 $P(\omega_1) = 0.9, P(\omega_2) = 0.1$，某项检测的类条件概率为 $p(x|\omega_1) = 0.2, p(x|\omega_2) = 0.4$，则根据最小错误率贝叶斯决策，观测到特征 $x$ 的样本应分类为：**
    A. 异常（$\omega_2$）
    B. 正常（$\omega_1$）
    C. 无法判断
    D. 同时属于两类

39. **最大后验概率（MAP）决策等价于哪一个贝叶斯决策准则？**
    A. 最小风险贝叶斯决策（当损失函数为0-1损失时）
    B. 最小方差估计
    C. 最大似然估计
    D. 最小类内散布准则

40. **设计二分类贝叶斯分类器时，若定义判别函数 $g(x) = P(\omega_1|x) - P(\omega_2|x)$，则决策规则为：**
    A. 若 $g(x) < 0$，则 $x \in \omega_1$
    B. 若 $g(x) > 0$，则 $x \in \omega_1$
    C. 若 $g(x) = 0$，则 $x \in \omega_1$
    D. 无论 $g(x)$ 为何值均判定为 $\omega_2$

41. **极大似然估计（MLE）的核心思想是：**
    A. 寻找一组参数，使得已发生的训练样本观测数据的联合概率（似然函数）最大
    B. 最小化所有类别的后验风险
    C. 寻找方差最大的方向进行投影降维
    D. 引入松弛变量来处理线性不可分数据

42. **多元正态分布参数化模型中，描述各个维度之间相关性的参数是：**
    A. 均值向量 $\mu_i$
    B. 权重系数 $P_j$
    C. 协方差矩阵 $\Sigma_i$
    D. 判别函数 $g(x)$

43. **高斯混合模型（GMM）的基本出发点是：**
    A. 抛弃一切概率模型，改用超平面分割
    B. 用多个高斯分布的加权线性组合来逼近并拟合任意复杂的连续概率密度分布
    C. 假定所有特征变量之间都严格独立
    D. 串行组合多个弱分类器

44. **在朴素贝叶斯分类器（Naive Bayes）中，“朴素”一词的根本含义是指：**
    A. 假设所有类别的先验概率完全相等
    B. 假设数据集没有任何噪声
    C. 强行假设样本的各个属性/特征之间在给定类别时是条件独立的
    D. 分类器结构非常简单，不需要进行任何参数学习

45. **朴素贝叶斯分类器引入“属性条件独立性假设”带来的直接技术优势是：**
    A. 极大地提高了模型的非线性表达能力
    B. 将高维联合概率密度的估计拆解为多个一维单概率密度的乘积，彻底简化了计算
    C. 能够自动完成特征提取工作
    D. 保证分类错误率绝对为零

46. **贝叶斯决策理论中，引入“损失函数（Loss Function）”的目的是为了处理：**
    A. 最小错误率决策
    B. 最小风险决策（考虑不同错误分类带来的代价差异）
    C. 特征提取中的维数灾难
    D. 过拟合问题

47. **全概率公式在贝叶斯决策中主要用于计算分母，即特征全概率 $p(x)$，其公式形式为：**
    A. $p(x) = \sum_{j=1}^c p(x|\omega_j)$
    B. $p(x) = \sum_{j=1}^c P(\omega_j)$
    C. $p(x) = \sum_{j=1}^c p(x|\omega_j)P(\omega_j)$
    D. $p(x) = \prod_{j=1}^c p(x|\omega_j)P(\omega_j)$

48. **当各类别的先验概率 $P(\omega_i)$ 完全相等，且各类的类条件概率密度均呈方差相同的正态分布时，最小错误率贝叶斯边界是：**
    A. 复杂的超球面
    B. 线性判定面（超平面）
    C. 抛物面
    D. 阶跃函数

49. **在手算朴素贝叶斯分类器时，如果某个离散特征值在训练集中未曾出现过，导致条件概率为0，通常采用什么技术进行平滑修正？**
    A. 极大似然估计
    B. 拉普拉斯修正（Laplace Correction / 加1平滑）
    C. 主成分分析
    D. 伪逆法

50. **最大后验概率（MAP）与最大似然估计（MLE）的主要区别在于，MAP考虑了：**
    A. 样本的几何特征
    B. 类别的先验概率分布
    C. 错分样本的动态权重
    D. 核函数的映射

51. **多元正态分布特征密度函数的指数项中，计算样本点到均值向量的距离被称为：**
    A. 欧氏距离
    B. 马氏距离
    C. 切比雪夫距离
    D. 闵可夫斯基距离

52. **贝叶斯分类器的根本技术弱点在于：**
    A. 缺乏坚实的数学理论支持
    B. 无法处理二分类问题
    C. 极度依赖并假定已知或能准确估计数据的概率分布
    D. 计算速度在所有分类算法中是最慢的

53. **在贝叶斯风险决策中，若把正确分类的损失设为0，错误分类的损失设为相同正值，则最小风险决策退化为：**
    A. 最小方差决策
    B. 最小错误率决策
    C. 最大似然决策
    D. 判别函数平方和最小决策

54. **在正态分布假设下，若各类的协方差矩阵不相等（$\Sigma_i \ne \Sigma_j$），则贝叶斯判定边界一般是：**
    A. 直线或超平面
    B. 二次或高次判定曲面
    C. 离散点集
    D. 平行于坐标轴的矩形

55. **贝叶斯公式中，起到将“已有先验知识”和“新观测到的证据”有机结合起来的纽带是：**
    A. 后验概率
    B. 散布矩阵
    C. 松弛变量
    D. 准则函数

### 模块4：线性判别函数与支持向量机（第56-75题）
56. **支持向量机（SVM）的核心思想与贝叶斯完全不同，它在特征空间中追求的是：**
    A. 拟合出精确的特征条件概率密度分布
    B. 寻找一条离两边数据几何边缘都尽可能远的“最优分割面”（最大化间隔）
    C. 通过串行学习逐步修正错分样本的权重
    D. 使得类内散布矩阵和类间散布矩阵的比值最大

57. **在硬间隔支持向量机的标准数学优化问题中，其最终的优化目标可以量化转化为：**
    A. $\max \|w\|$
    B. $\min \frac{1}{2}\|w\|^2$
    C. $\min \sum \xi_i$
    D. $\max w^T x + b$

58. **硬间隔支持向量机要求所有训练样本必须严格满足的约束条件是：**
    A. $y_i(w^T x_i + b) \ge 1$
    B. $y_i(w^T x_i + b) \le 0$
    C. $\sum \alpha_i = 0$
    D. $w^T x_i + b = 0$

59. **现实中当训练数据线性不可分或者存在噪声时，支持向量机引入了什么机制来构建“软间隔（Soft Margin）”？**
    A. 类条件独立性假设
    B. 松弛变量 $\xi_i$ 与惩罚因子 $C$（Hinge Loss）
    C. 串行弱分类器投票
    D. 协方差矩阵特征值分解

60. **软间隔支持向量机优化目标中，参数 $C$（惩罚因子）的作用是：**
    A. 控制投影后特征空间的实际维度
    B. 调节“最大化几何间隔”与“减少错分样本（违反约束的程度）”之间的折中平衡
    C. 设定核函数的最高阶数
    D. 决定最近邻搜索的半径圆大小

61. **支持向量机中，决定最终分类超平面边界的样本是：**
    A. 所有的训练集样本
    B. 远离判定边界的极值样本
    C. 落在间隔边界上或之内的“支持向量（Support Vectors）”
    D. 被噪声严重污染的离群点

62. **当面对非线性分类问题时，Kernel SVM（核支持向量机）利用核技巧实现了：**
    A. 自动抛弃非线性特征，只保留线性特征
    B. 将弱分类器串行集成起来提升性能
    C. 在低维空间中直接计算高维映射后的内积，从而高效解决非线性可分问题
    D. 动态更新训练样本的概率密度函数

63. **感知器算法（Perceptron）的核心优化路线是：**
    A. 利用广义特征值分解求解逆矩阵
    B. 直接定义判别函数 $g(x) = w^T x + b$，利用梯度下降迭代修正错分样本的权重向量
    C. 自顶向下构建复杂的规则树
    D. 计算样本到中心的平方和准则

64. **关于感知器算法的收敛性，下列结论绝对正确的是：**
    A. 无论数据分布如何，感知器算法均能在有限步内完美收敛
    B. 当且仅当训练样本数据是线性可分时，感知器算法保证收敛
    C. 数据线性不可分时，感知器依然能收敛到全局最优解
    D. 感知器的收敛速度完全取决于类别先验概率

65. **当数据被证实为线性不可分、导致感知器无法收敛时，通常可以改用什么方法来求解线性判别函数？**
    A. 最小平方误差判别（MSE，利用伪逆法或梯度下降求解）
    B. 肘部法则
    C. 极大似然估计
    D. 层次分解算法

66. **在线性判别函数中，若样本被正确分类，则感知器准则函数 $J_p(w)$ 的值应当：**
    A. 趋于无穷大
    B. 等于零（因为错分样本集为空）
    C. 持续线性增长
    D. 变为负数

67. **为了解决多分类问题，将所有类别两两配对训练 $\frac{c(c-1)}{2}$ 个分类器的SVM扩展策略被称为：**
    A. OVR（一对多，One-vs-Rest）
    B. OVO（一对一，One-vs-One）
    C. 直接多分类策略
    D. 层次树状分类

68. **支持向量机（SVM）中，若选用高斯核函数（RBF），其潜在的作用是将特征映射到：**
    A. 零维空间
    B. 保持原始低维空间不变
    C. 无穷维空间
    D. 二维平面

69. **感知器算法是一种什么样的学习方法？**
    A. 增量式/在线学习算法（每次用错分样本更新）
    B. 批量全局矩阵分解算法
    C. 无监督聚类算法
    D. 非参数化密度估计算法

70. **线性判别函数 $g(x) = w^T x + b = 0$ 几何上代表特征空间中的：**
    A. 一个弯曲的超曲面
    B. 超平面（Decision Hyperplane）
    C. 一个闭合的超球面
    D. 散点集合

71. **在最小平方误差判别（MSE）中，利用伪逆法求解权重向量 $w$ 的核心公式涉及到：**
    A. 广义特征值分解
    B. 伪逆矩阵 $X^{\dagger} = (X^T X)^{-1} X^T$ 的计算
    C. 核函数的内积映射
    D. 概率密度的乘积

72. **SVM中的“硬间隔”和“软间隔”最本质的区别在于：**
    A. 是否允许训练样本在一定程度上违反边际约束
    B. 是否使用了高维非线性核函数
    C. 优化目标是最小化方差还是最大化方差
    D. 分类器属于有监督还是无监督学习

73. **如果SVM模型在训练集上达到了100%的准确率，但在测试集上表现极差，应当如何调整惩罚参数 $C$？**
    A. 继续无限增大 $C$
    B. 适当减小 $C$，允许一定的训练误差以增强泛化能力
    C. 将 $C$ 设为0
    D. 变换为硬间隔模型

74. **支持向量机（SVM）的数学基础来源于：**
    A. 统计学习理论与凸优化理论
    B. 全概率公式与马尔可夫链
    C. 模糊数学与图论
    D. 仿生学与集成进化理论

75. **感知器算法中，权重更新的方向是沿着准则函数的：**
    A. 梯度正方向
    B. 负梯度方向（即梯度下降法）
    C. 正交切线方向
    D. 随机扰动方向

### 模块5：集成学习与提升方法（第76-85题）
76. **提升学习（Boosting）方法的核心内在逻辑是：**
    A. 让多个完全独立的分类器同时并行计算，最后取平均
    B. 串行组合多个弱分类器，每轮重点关注并加大前一轮被错分样本的权重
    C. 自动抛弃表现不好的特征，实现特征选择
    D. 自底向上将样本逐步合并成一棵大的聚类树

77. **下列关于Boosting方法特点的说法中，错误的是：**
    A. 个体学习器之间存在强依赖关系，必须串行学习
    B. 每一轮集成后的强分类器性能通常都比前一轮更好
    C. 新增加的个体学习器重点关注被正确分类的样本
    D. 个体学习器之间是非独立的

78. **在经典的AdaBoost算法中，第 $t$ 轮基分类器的投票权重 $\alpha_t$ 的计算公式为：**
    A. $\alpha_t = \frac{1}{2} \ln \frac{1 - \varepsilon_t}{\varepsilon_t}$
    B. $\alpha_t = \ln \frac{\varepsilon_t}{1 - \varepsilon_t}$
    C. $\alpha_t = \sqrt{1 - \varepsilon_t}$
    D. $\alpha_t = \frac{1}{\varepsilon_t}$

79. **在AdaBoost权重计算实例中，若第3轮分类器的错误率 $\varepsilon_3 = 0.17$，则其计算出的投票权重 $\alpha_3$ 约为：**
    A. 0.17
    B. 0.50
    C. 0.793
    D. 1.25

80. **根据AdaBoost算法，如果一个弱分类器的分类错误率 $\varepsilon_t > 0.5$，通常意味着：**
    A. 该分类器极其优秀，应该赋予极高权重
    B. 它的表现甚至不如随机猜测，需要进行取反或重新训练
    C. 算法直接完美收敛停止
    D. 样本权重需要重置为均匀分布

81. **AdaBoost最终强分类器的输出形式是通过以下哪种方式产生的？**
    A. 所有弱分类器输出结果的简单不加权算术平均
    B. 剔除所有错误率高的分类器，只保留最后训练的那一个
    C. 各弱分类器基于其投票权重 $\alpha_t$ 进行加权多数表决
    D. 随机抽取一个弱分类器的输出作为最终结果

82. **Bagging方法与Boosting方法在个体学习器生成机制上的根本区别是：**
    A. Bagging是有监督学习，Boosting是无监督学习
    B. Bagging是个体学习器独立并行生成，Boosting是个体学习器串行强依赖生成
    C. Bagging基于概率分布，Boosting基于几何超平面
    D. Bagging会改变样本空间维度，Boosting不会

83. **在AdaBoost算法的样本权重更新步骤中，若样本 $x_i$ 在第 $t$ 轮被正确分类，则其在下一轮的权重 $D_{t+1}(i)$ 将：**
    A. 保持绝对不变
    B. 被成倍放大
    C. 被相应缩小，使其在后续训练中受关注度降低
    D. 直接归零

84. **随着训练轮数 $T$ 的不断增加，AdaBoost强分类器在训练集上的训练误差通常会：**
    A. 呈指数级快速下降直至趋近于0
    B. 线性稳步上升
    C. 保持完全不变
    D. 呈现出剧烈的随机上下震荡

85. **集成学习之所以能够提升分类性能，其底层的哲学思想是：**
    A. 降低数据的采样频率
    B. 结合多个“三个臭皮匠”式的弱分类器，互补不足，构造出一个“诸葛亮”式的强分类器
    C. 完全消除特征之间的相关性
    D. 将非线性分类问题转化为无监督的聚类问题

### 模块6：特征选择与特征提取（第86-100题）
86. **特征选择与特征提取的根本区别在于：**
    A. 特征选择需要先验知识，特征提取完全不需要
    B. 特征选择是筛选原始特征的子集（保留原貌），特征提取是通过数学变换生成全新的新特征
    C. 特征选择是生成新的特征组合，特征提取是单纯剔除噪声
    D. 特征选择适用于线性数据，特征提取适用于非线性数据

87. **进行特征选择/提取降维的最直接驱动技术动因是为了克服：**
    A. 局部最优解
    B. 维数灾难（Curse of Dimensionality，维度高导致样本稀疏、计算困难及过拟合）
    C. 感知器不收敛
    D. 随机权重的初始化震荡

88. **特征选择三大判据中，基于类内类间散布距离的代表性判据是：**
    A. 类间平均距离判据 $J_d(x)$
    B. 基于后验概率推导的熵判据
    C. 交叉熵损失函数
    D. 贝叶斯最小风险值

89. **可分性准则 $J_d(x)$ 必须具备的四条优良性质中，包括加特征时判据值“不降”的性质，这被称为：**
    A. 可加性
    B. 单调性
    C. 唯一性
    D. 齐次性

90. **特征提取算法PCA（主成分分析）的核心技术目标是：**
    A. 寻找让不同类别之间离得最远的投影轴
    B. 寻找数据中方差最大的方向做投影，从而在降维同时尽可能保留原始信息
    C. 利用非线性核函数将数据映射到无穷维空间
    D. 极大化多类别的后验风险

91. **主成分分析（PCA）计算流程的第一步、也是绝对不可或缺的一步是：**
    A. 求解特征值与特征向量
    B. 对原始数据进行去中心化（每个样本减去全体均值向量 $\mu$）
    C. 计算类间散布矩阵 $S_b$
    D. 设定软间隔松弛变量

92. **在PCA中，样本去中心化后算得的矩阵 $C_X = \frac{1}{N}\sum \tilde{x}_i \tilde{x}_i^T$ 被称为：**
    A. 类内散布矩阵
    B. 协方差矩阵（Covariance Matrix）
    C. 恒等矩阵
    D. 转换矩阵

93. **PCA降维时，所获得的“第一主成分”在数学上对应于：**
    A. 协方差矩阵最小特征值对应的特征向量
    B. 协方差矩阵最大特征值对应的特征向量
    C. 类内散布矩阵的逆矩阵
    D. 随机抽取的特征向量

94. **在PCA应用中，特征值 $\lambda_j$ 的物理意义对应于：**
    A. 样本分类的错误率
    B. 数据在新主成分投影轴方向上的方差（$\lambda$ 越大保留信息越多）
    C. 类别中心之间的距离
    D. 核函数的平滑参数

95. **线性判别分析（LDA / Fisher线性判别）的最核心技术特色是：**
    A. 它是完全无监督的降维方法，不依赖任何类别标签
    B. 它是强监督的降维方法，利用类别标签寻找让不同类别“分离最好”的投影方向
    C. 它的目标是最大化投影后的总样本方差
    D. 它通过计算熵来过滤多余特征

96. **Fisher线性判别（LDA）量化投影质量的优化目标准则是：**
    A. 让类间离散度 $S_b$ 尽可能小，类内离散度 $S_w$ 尽可能大
    B. 让类间离散度 $S_b$ 尽可能大，类内离散度 $S_w$ 尽可能小（极大化比值 $\frac{w^T S_b w}{w^T S_w w}$）
    C. 最小化样本到类中心的平方和
    D. 最小化错分样本的加权和

97. **LDA（线性判别分析）在数学上最终被转化为求解什么问题？**
    A. 拉格朗日对偶问题
    B. 广义特征值分解问题（矩阵 $S_w^{-1}S_b$ 的最大特征值对应特征向量）
    C. 伪逆矩阵的代数乘积
    D. 肘部拐点的定性分析

98. **关于PCA与LDA的核心区别，下列说法中正确的是：**
    A. PCA是有监督的，LDA是无监督的
    B. PCA关注于数据的“最大方差保持”，LDA关注于类别的“最大可分性投影”
    C. PCA只能处理二分类，LDA可以处理任意多分类
    D. PCA计算复杂度显著高于LDA

99. **PCA在人脸识别领域的经典实战应用被称为：**
    A. 稀疏人脸
    B. 特征人脸（Eigenfaces）
    C. 判别人脸
    D. 线性人脸

100. **在特征选择搜索策略中，从空集开始、每次引入一个使得可分性判据最优的特征的方法称为：**
     A. 序列向前选择（SFS）
     B. 序列向后选择（SBS）
     C. 穷举搜索
     D. 随机森林筛选

---

## 二、多项选择题（共50题，每题2分，多选、少选、错选均不得分）

### 模块1：模式识别与基础（第101-105题）
101. **一个完整的模式识别系统通常包含下列哪些关键的组成部分？**
     A. 模式采集与数据获取
     B. 数据预处理
     C. 特征选择与特征提取
     D. 分类器设计/聚类判定

102. **下列考点属于模式识别有监督分类（Supervised Classification）范畴的有：**
     A. 贝叶斯决策方法
     B. 支持向量机（SVM）
     C. K-Means算法
     D. AdaBoost集成学习

103. **在模式识别中，引发过拟合（Overfitting）现象的主要原因包括：**
     A. 训练样本数量过少、缺乏代表性
     B. 分类器模型过于复杂，参数过多
     C. 训练过程中过度拟合了数据中的噪声
     D. 进行了数据的去中心化操作

104. **下列应用领域中，深度融合了模式识别核心算法的有：**
     A. 自动驾驶多传感器融合环境感知
     B. 大语言模型（LLM）的文本理解与推理
     C. 生物特征识别（指纹、人脸、虹膜）
     D. 计算机网络硬件拓扑路由物理架设

105. **关于特征空间（Feature Space），下列说法正确的有：**
     A. 特征空间的每一个维度对应对象的一个测量属性
     B. 空间中的每一个点代表一个具体的对象样本
     C. 维度越高，样本分布越紧密，越有利于贝叶斯估计
     D. 判定边界的几何形状直接受到特征空间分布的影响

### 模块2：模式聚类分析（第106-120题）
106. **聚类分析中，影响最终聚类结果走向的根本决定性因素有：**
     A. 特征的选取与定义
     B. 相似度（距离度量）的定义
     C. 聚类准则的设定
     D. 运行算法时使用的计算机内存大小

107. **现代模式识别中，常用的四大类基础聚类方法包括：**
     A. 试探法（如最近邻规则）
     B. 层次法（包括融合与分解）
     C. 动态法（如K-Means、ISODATA）
     D. 密度法（如DBSCAN）

108. **下列关于最近邻规则试探法聚类的叙述，正确的有：**
     A. 算法极其简单直观，具备在线增量学习能力
     B. 聚类结果严重受到样本输入先后顺序的影响
     C. 对聚类圆半径阈值 $T$ 的选择极其敏感
     D. 能够完美识别具有极其复杂几何形状的簇

109. **关于层次聚类中的“融合算法”，下列步骤正确的有：**
     A. 初始状态将 $n$ 个样本各自独立看作一个类
     B. 计算各两类之间的类间距离，合并最相似的两个类
     C. 类别数由 $n$ 逐步递减至满足要求的 $K$
     D. 具有不可逆性，一旦合并无法在后续步骤中无损拆分

110. **K-Means算法在实际运行中，常见的技术痛点或局限性包括：**
     A. 需要预先人为指定类别数 $K$
     B. 结果容易受到随机选取的初始聚类中心的影响
     C. 准则函数 $E$ 容易陷入局部最优解
     D. 无法处理具有非球形、复杂几何形状的数据分布

111. **ISODATA算法在K-Means的基础上进行了大幅升级，它能够动态执行的操作有：**
     A. 当两个中心距离过近时，自动执行“合并”
     B. 当某一类内标准差过大时，自动执行“分裂”
     C. 自动调整类别数 $K$，无需指定任何初始控制参数
     D. 在迭代过程中动态剔除包含样本数过少的“噪声类”

112. **DBSCAN基于密度的聚类算法中，涉及到的核心技术参数与概念有：**
     A. 邻域半径参数 $\varepsilon$
     B. 最小邻居数阈值 $MinPts$
     C. 核心点与噪声点
     D. 肘部弯曲拐点

113. **关于DBSCAN算法的优缺点，下列说法正确的有：**
     A. 优点是不需要预先指定聚类的簇数 $K$
     B. 优点是能够极其敏锐地发现任意形状的簇并隔离噪声
     C. 缺点是当数据密度极不均匀时，单一的 $\varepsilon$ 和 $MinPts$ 难以获得理想效果
     D. 缺点是计算量在任何场景下都显著高于分解层次聚类

114. **下列哪些公式或方法可以用于聚类分析中的样本相似度/距离度量？**
     A. 欧氏距离（L2距离）
     B. 曼哈顿距离（L1距离）
     C. 夹角余弦相似度
     D. 判别函数偏导数

115. **在ISODATA算法的合并步骤中，必须同时满足哪些条件或流程？**
     A. 两个中心的几何距离必须小于合并阈值 $\theta_c$
     B. 满足条件的配对按距离从小到大严格排列，依次执行合并
     C. 合并后的新中心必须通过两个旧中心的样本数加权平均算出
     D. 必须保证合并后的总类别数大于初始设定的Nc

116. **导致聚类分析被定义为“不可逆”操作的原因包括：**
     A. 层次融合后，原始样本之间的个体特殊独立关系被类统一标签取代
     B. 层次分解后，子类之间失去了再次直接融合成完全原貌的关联信息
     C. 聚类准则函数的逼近过程通常是多对一的映射降维
     D. 聚类算法完全无法在计算机中运行反向迭代

117. **在K-Means中，判定算法已经成功收敛的科学依据有：**
     A. 准则函数 $E$ 的数值变化量连续低于预设的极小误差限
     B. 所有的类中心在前后两轮迭代中完全不再移动
     C. 每个样本所属的类别标签完全固化，不再发生切换
     D. 迭代次数直接触发了人为设定的最大上限

118. **下列应用场景中，属于聚类算法典型落地领域的有：**
     A. 电子商务系统对用户购买行为进行自动价值分群
     B. 搜索引擎根据检索内容的相似性对搜索结果进行自动聚类成组
     C. 垃圾邮件基于标注库的二分类拦截
     D. 遥感图像中对未标明地物的区域进行自动光谱谱系分组

119. **关于DBSCAN算法中的点分类，下列说法正确的有：**
     A. 核心点的 $\varepsilon$ 邻域内包含的样本数 $\ge MinPts$
     B. 边界点的邻域样本数 $< MinPts$，但它落在某个核心点的 $\varepsilon$ 邻域内
     C. 噪声点的邻域样本数 $< MinPts$，且不属于任何核心点的邻域
     D. 所有未被分配给核心点的点都属于聚类中心

120. **在定义类间距离时，层次聚类常用的度量准则包括：**
     A. 最短距离法（Single Linkage）
     B. 最长距离法（Complete Linkage）
     C. 平均距离法（Average Linkage）
     D. 随机距离法

### 模块3：统计决策方法与贝叶斯分类（第121-135题）
121. **贝叶斯决策理论三大支柱准则包括：**
     A. 最小错误率准则
     B. 最小风险准则
     C. 最大后验概率准则
     D. 最大似然估计准则

122. **利用贝叶斯公式进行分类器设计时，为了完整计算后验概率 $P(\omega_i|x)$，必须已知或估计出的概率参数有：**
     A. 各类别的先验概率 $P(\omega_i)$
     B. 对应类别的类条件概率密度 $p(x|\omega_i)$
     C. 特征全概率（特征空间的总概率密度） $p(x)$
     D. 弱分类器的错误率权重 $\alpha_t$

123. **关于贝叶斯决策中的“先验概率”与“后验概率”，下列辨析正确的有：**
     A. 先验概率是在未看观测样本特征前，基于历史经验或统计得到的类别比例
     B. 后验概率是在观测到具体样本特征 $x$ 后，更新得到的该样本属于各类的概率
     C. 先验概率通过后验概率乘以似然函数直接求得
     D. 后验概率是贝叶斯分类进行最终决策划分的直接依据

124. **朴素贝叶斯分类器（Naive Bayes）之所以被称为“朴素”，其核心假设条件包括：**
     A. 特征与特征之间在给定类别下是条件独立的，没有任何协同关联
     B. 数据的联合概率密度等于各个特征边缘概率密度的乘积
     C. 各类别的协方差矩阵必须完全等同于单位矩阵
     D. 所有的先验概率必须完全相等

125. **高斯混合模型（GMM）的组成要素包括：**
     A. 多个独立的高斯（正态）分布成分
     B. 各个高斯成分对应的权重系数（先验混合比例 $P_j$）
     C. 每个高斯成分自身的均值向量 $\mu_j$ 和协方差矩阵 $\Sigma_j$
     D. 错分样本的指数级加权系数

126. **关于极大似然估计（MLE）与最大后验概率估计（MAP）的叙述，正确的有：**
     A. MLE不考虑参数自身的先验分布，视参数为未知但固定的常数
     B. MAP将参数视为具有某种先验分布的随机变量，结合了先验与似然
     C. 当参数的先验分布表现为均匀分布时，MAP的求解结果与MLE完全等价
     D. MLE和MAP都不需要使用任何训练样本数据

127. **多元正态分布参数化模型中，若假定所有类别共享完全相同的协方差矩阵（$\Sigma_i = \Sigma$），则：**
     A. 贝叶斯决策边界会呈现为线性超平面
     B. 二次项判定边界将彻底退化、消除
     C. 判别函数变为关于特征 $x$ 的线性函数
     D. 必须改用支持向量机才能进行分类

128. **在二分类最小错误率贝叶斯决策中，下列哪些表达式等价于“判定样本 $x$ 属于 $\omega_1$”？**
     A. $P(\omega_1|x) > P(\omega_2|x)$
     B. $p(x|\omega_1)P(\omega_1) > p(x|\omega_2)P(\omega_2)$
     C. $\frac{p(x|\omega_1)}{p(x|\omega_2)} > \frac{P(\omega_2)}{P(\omega_1)}$
     D. $P(\omega_1|x) < P(\omega_2|x)$

129. **统计决策方法中，设计最小风险贝叶斯分类器时必不可少的要素有：**
     A. 完整的类条件概率密度分布
     B. 明确给定的先验概率
     C. 量化每种错分后果代价的损失矩阵（Loss Matrix $\lambda_{ij}$）
     D. 核函数的内积映射矩阵

130. **朴素贝叶斯分类器在实际工程落地中的核心优势包括：**
     A. 算法结构极其简单，计算速度极快，易于大规模并行扩展
     B. 在小规模或高维稀疏数据集（如文本垃圾邮件分类）上表现出惊人的健壮性
     C. 参数估计极其简单，只需分别统计一维特征的概率分布即可
     D. 能够完美拟合特征之间存在强相互依赖的复杂非线性系统

131. **在概率密度函数估计中，参数化估计与非参数化估计的核心区别在于：**
     A. 参数化估计预先假设数据服从某种特定形式的已知分布（如多元正态分布）
     B. 非参数化估计不强制约束分布的形式，直接从数据样本出发逼近密度函数
     C. 参数化估计只需要估计少量的参数（如 $\mu, \Sigma$）
     D. 非参数化估计绝对无法用于贝叶斯分类器的构建

132. **由于拉普拉斯修正（Laplace Correction）在朴素贝叶斯中的应用，它可以：**
     A. 防止分子或分母由于样本未出现而出现0概率，导致整个乘积公式失效
     B. 为每个特征项的频数计数加上一个微小的正数平滑项
     C. 消除模型的过拟合，提高泛化能力
     D. 将有监督模型直接自动转换为无监督模型

133. **多元正态分布特征概率密度函数中，协方差矩阵 $\Sigma$ 的性质必须满足：**
     A. 它必须是一个实对称矩阵
     B. 它必须满足正定性（或半正定），行列式 $|\Sigma| > 0$
     C. 矩阵的对角线元素代表各个独立维度的自身方差
     D. 矩阵的所有元素必须全部为正数

134. **关于贝叶斯公式 $P(\omega_i|x) = \frac{p(x|\omega_i)P(\omega_i)}{p(x)}$ 的内涵理解，正确的有：**
     A. 分母 $p(x)$ 是全概率，在比较不同类别后验大小时起到相同的归一化常数作用
     B. 分子是该类别的似然度与先验概率的乘积
     C. 它完美地将客观观测（似然）与主观先验知识融合在一起
     D. 它能够直接消除训练集中的所有噪声干扰

135. **在最小风险贝叶斯决策中，决策条件是选择使期望损失（风险）最小的类别，其数学表达涉及到：**
     A. 条件风险 $R(\alpha_i|x) = \sum_{j=1}^c \lambda_{ij} P(\omega_j|x)$
     B. 比较各个可行动作的条件风险值，选择风险最小者
     C. 必须使用 Hinge Loss 作为唯一的损失函数
     D. 风险大小直接受到损失矩阵 $\lambda$ 具体赋值的影响

### 模块4：线性判别与支持向量机（第136-145题）
136. **支持向量机（SVM）在处理分类优化问题时，其核心技术路径包括：**
     A. 最大化分类几何间隔（Max Margin）
     B. 建立拉格朗日函数，将其转化为对偶问题（Dual Problem）进行求解
     C. 引入KKT条件（Karush-Kuhn-Tucker）以保证全局最优化
     D. 强行假设特征之间满足条件独立性

137. **关于支持向量（Support Vectors），下列说法正确的有：**
     A. 它们是距离分类判定超平面最近的那些关键边界训练样本
     B. 它们的拉格朗日乘子 $\alpha_i > 0$
     C. 若从数据集中剔除所有非支持向量样本、重新训练，最终获得的超平面完全不变
     D. 支持向量只存在于硬间隔SVM中，软间隔模型不包含支持向量

138. **软间隔支持向量机引入松弛变量 $\xi_i$ 的核心技术动因包括：**
     A. 现实中训练样本数据几乎不可能绝对线性可分
     B. 容忍数据中存在一定程度的离群噪声，避免模型产生严重的过拟合
     C. 能够使SVM分类超平面的求解问题始终保持良态的凸优化性质
     D. 能够自动降低原始特征空间的物理维度

139. **下列哪些属于支持向量机（SVM）中常用的经典核函数（Kernel Functions）？**
     A. 线性核函数（Linear Kernel）
     B. 多项式核函数（Polynomial Kernel）
     C. 高斯径向基核函数（RBF / Gaussian Kernel）
     D. 贝叶斯全概率核函数

140. **关于感知器算法（Perceptron），下列说法正确的有：**
     A. 其目标是寻找一个权重向量 $w$ 和偏置 $b$，建立判别函数 $g(x) = w^T x + b$
     B. 优化准则函数基于错分样本到超平面的加权距离之和
     C. 采用负梯度方向进行迭代权重更新（梯度下降）
     D. 如果数据是线性不可分的，算法将永远处于震荡状态、无法收敛

141. **为了解决线性不可分导致感知器不收敛的缺陷，最小平方误差（MSE）判别函数：**
     A. 引入了错误分类的残差平方和作为全新准则函数
     B. 可以直接通过求导并利用伪逆法（广义逆）进行闭式解的一步求解
     C. 也可以采用梯度下降法进行渐进式迭代求解
     D. 要求各类别的协方差矩阵必须严格相等

142. **在SVM多分类扩展中，“一对多（OVR）”策略与“一对一（OVO）”策略的区别包括：**
     A. OVR需要训练 $c$ 个分类器，OVO需要训练 $\frac{c(c-1)}{2}$ 个分类器
     B. OVR的每个基分类器都使用全体训练样本，OVO的每个基分类器仅使用对应的两类样本
     C. OVO容易受到样本类别不平衡的严重干扰，OVR则完全不存在该缺陷
     D. OVO在类别数 $c$ 极大时，需要训练的基分类器数量会发生爆发式增长

143. **支持向量机优化目标中的惩罚因子 $C$，其取值对模型性能的影响表现为：**
     A. $C$ 趋于无穷大时，模型逼近硬间隔SVM，极易过拟合
     B. $C$ 取值过小时，模型对错分样本惩罚极弱，间隔可能很大但会导致欠拟合
     C. 调整 $C$ 能够精细调节模型在“训练集误差”与“测试集泛化能力”之间的折中
     D. $C$ 的取值直接决定了高斯核函数的映射维度

144. **关于线性判别函数中的判定超平面 $w^T x + b = 0$，其几何性质表现为：**
     A. 权重向量 $w$ 垂直于判定超平面，决定了超平面的法线方向
     B. 偏置 $b$ 决定了超平面相对于坐标原点的位移距离
     C. 任意样本点 $x$ 到该超平面的代数距离正比于 $|w^T x + b|$
     D. 超平面将整个特征空间完美切分为正类区域（$g(x)>0$）和负类区域（$g(x)<0$）

145. **感知器算法准则函数 $J_p(w)$ 具有哪些数学特点？**
     A. 它是一个非负的连续函数
     B. 在错分样本点处是分段线性的
     C. 它是一个严格的凸函数，因此局部极小值即为全局最小值
     D. 它的最小值在所有样本分类完全正确时达到，此时函数值严格等于0

### 模块5：集成学习与特征选择/提取（第146-150题）
146. **提升学习中经典的AdaBoost算法，其每轮迭代完整涉及到的核心步骤包括：**
     A. 初始化训练样本的均匀概率权重分布 $D_1$
     B. 在当前权重分布下训练基弱分类器，并计算其分类错误率 $\varepsilon_t$
     C. 依据错误率计算当前弱分类器的投票权重 $\alpha_t$
     D. 更新样本权重分布，大幅提高错分样本权重，缩小正确分类样本权重，并进行归一化

147. **关于特征选择与特征提取的底层考点辨析，正确的有：**
     A. 特征选择不改变特征的原始物理意义，仅仅挑出最优子集
     B. 特征提取通过空间映射/变换，产生全新物理意义的组合特征
     C. 两者的共同目标都是降低特征维度，克服维数灾难，提升分类性能
     D. 特征选择最适合非线性结构，特征提取绝对无法处理非线性结构

148. **主成分分析（PCA）算法之所以能够广泛用于降维与信息保持，其数学保障在于：**
     A. 投影后的第一主成分具有数据重构误差最小的优良性质
     B. 投影轴方向成功对应了数据样本投影后方差最大的方向
     C. 各个主成分之间彼此正交，完全消除了原始特征中的冗余相关性
     D. 它是一种强监督的分类边界查找算法

149. **Fisher线性判别（LDA）与主成分分析（PCA）的同异点辨析包括：**
     A. 相同点：两者都属于线性降维技术，都通过投影矩阵将高维空间变换到低维空间
     B. 不同点：PCA是无监督的、关注全局最大方差；LDA是有监督的、关注类别间的最大分离
     C. 不同点：PCA降维后的维度受原始特征限制，LDA降维后的维度最多为 $c-1$
     D. 相同点：两者都强制要求属性之间满足朴素贝叶斯的条件独立性假设

150. **特征选择搜索策略中，属于启发式/次优搜索策略的有：**
     A. 序列向前选择（SFS，一前向加特征）
     B. 序列向后选择（SBS，一后向剔特征）
     C. 穷举搜索（Exhaustive Search，彻底遍历所有组合）
     D. 双向搜索（Bidirectional Search）

---

## 三、参考答案与核心解析速查表

### 1. 单项选择题（1-100题）答案速查
| 题号 | 答案 | 题号 | 答案 | 题号 | 答案 | 题号 | 答案 | 题号 | 答案 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **1** | B | **2** | B | **3** | B | **4** | C | **5** | C |
| **6** | A | **7** | C | **8** | B | **9** | C | **10** | B |
| **11** | A | **12** | C | **13** | B | **14** | B | **15** | B |
| **16** | A | **17** | B | **18** | B | **19** | C | **20** | B |
| **21** | B | **22** | C | **23** | B | **24** | B | **25** | C |
| **26** | B | **27** | B | **28** | B | **29** | C | **30** | C |
| **31** | C | **32** | B | **33** | B | **34** | B | **35** | C |
| **36** | A | **37** | C | **38** | B | **39** | A | **40** | B |
| **41** | A | **42** | C | **43** | B | **44** | C | **45** | B |
| **46** | B | **47** | C | **48** | B | **49** | B | **50** | B |
| **51** | B | **52** | C | **53** | B | **54** | B | **55** | A |
| **56** | B | **57** | B | **58** | A | **59** | B | **60** | B |
| **61** | C | **62** | C | **63** | B | **64** | B | **65** | A |
| **66** | B | **67** | B | **68** | C | **69** | A | **70** | B |
| **71** | B | **72** | A | **73** | B | **74** | A | **75** | B |
| **76** | B | **77** | C | **78** | A | **79** | C | **80** | B |
| **81** | C | **82** | B | **83** | C | **84** | A | **85** | B |
| **86** | B | **87** | B | **88** | A | **89** | B | **90** | B |
| **91** | B | **92** | B | **93** | B | **94** | B | **95** | B |
| **96** | B | **97** | B | **98** | B | **99** | B | **100** | A |

### 2. 多项选择题（101-150题）答案速查
| 题号 | 标准答案 | 核心考点说明 |
|:---:|:---:|---|
| **101** | ABCD | 模式识别系统的四大通用串行基本骨架模块。 |
| **102** | ABD | 有监督分类包含标签已知的所有决策方法；C项K-Means为无监督聚类。 |
| **103** | ABC | 过拟合由样本过少、模型过繁、强记噪声引起；D项去中心化无影响。 |
| **104** | ABC | 计算机视觉识别、大语言模型和知识图谱均属于典型应用；D项属于网络纯硬件布线。 |
| **105** | ABD | 特征空间的概念定义与边界几何原理。高维可能引发样本稀疏（维数灾难）。 |
| **106** | ABC | 聚类的三大核心依赖驱动。计算机内存不改变聚类算法本身的数学走向。 |
| **107** | ABCD | 期末核心复习大纲强调的聚类分析四大基本分类方法。 |
| **108** | ABC | 最近邻试探法的基本属性。它使用单一标准，不适合复杂形状。 |
| **109** | ABCD | 层次聚类自底向上融合算法的标准流程与重要不可逆性质。 |
| **110** | ABCD | K-Means的全部典型缺陷。对初始点敏感、易入局部最优、K值难定、倾向球形簇。 |
| **111** | ABD | ISODATA根据动态阈值指标自动执行合并、分裂和清除噪声小类的机制。 |
| **112** | ABC | DBSCAN密度聚类的两大输入控制参数以及三个核心点定义类别。 |
| **113** | ABC | DBSCAN的优缺点辨析。它计算量前期通常小于自顶向下层次分解。 |
| **114** | ABC | 相似度度量涵盖欧氏、曼哈顿、余弦等。D项偏导数用于梯度更新。 |
| **115** | ABC | ISODATA合并的两大指标限制（距离和样本数加权更新）。 |
| **116** | ABC | 聚类不可逆性的底层数学成因：合并后不可分，分解后不可合。 |
| **117** | ABC | K-Means算法收敛的标准判定指标（中心不移动、标签不切换、误差稳定）。 |
| **118** | ABD | 客户分群、检索结果聚类、未知地物分组均无标签，属于聚类；C项属于有监督分类。 |
| **119** | ABC | DBSCAN基于邻域密度对空间数据点的三大精准定义。 |
| **120** | ABC | 层次聚类在计算两簇之间距离时最常用的三种统计经典准则。 |
| **121** | ABC | 贝叶斯统计决策方法的三大经典核心准则（最大似然估计属于参数估计范畴）。 |
| **122** | ABC | 贝叶斯公式的标准代数组成元素（后验 = 似然 × 先验 / 全概率）。 |
| **123** | ABD | 先验与后验概率的核心时间节点与逻辑辨析。 |
| **124** | AB | 朴素贝叶斯的“朴素”完全体现在特征条件独立性假设和全概率拆解上。 |
| **125** | ABC | 高斯混合模型（GMM）作为非参数/混合密度估计的三个核心数学参数。 |
| **126** | ABC | MLE（点估计）与MAP（结合先验的随机估计）在参数估计中的本质关联与差异。 |
| **127** | ABC | 多元正态分布下，共享协方差矩阵将直接导致判定边界中的二次项抵消，退化为线性超平面。 |
| **128** | ABC | 最小错误率贝叶斯决策规则的三种等价数学代数变换形式。 |
| **129** | ABC | 最小风险决策必须引入损失矩阵 $\lambda$ 来量化错分类别的实际代价成本。 |
| **130** | ABC | 朴素贝叶斯由于假设独立而带来的高速计算和对高维稀疏特征的极强鲁棒性。 |
| **131** | ABC | 参数化估计与非参数化估计的本质理论区别。非参数估计依然是为了计算贝叶斯类条件概率。 |
| **132** | AB | 拉普拉斯修正（加1平滑）的核心作用是防止频数为0导致整条概率相乘链条坍塌。 |
| **133** | ABC | 协方差矩阵的严格数学性质（实对称、正定、对角线为各自方差）。相关系数可正可负。 |
| **134** | ABC | 贝叶斯公式在模式分类应用中的底层哲学内涵与各乘项作用。 |
| **135** | ABD | 条件风险公式的精确代数定义与计算流程。损失函数不限于Hinge Loss。 |
| **136** | ABC | 支持向量机（SVM）硬间隔/软间隔标准二次凸规划问题的核心求解路径。 |
| **137** | ABC | 支持向量的标志性物理意义（边界决定性、拉格朗日乘子非零性）。软间隔中同样存在。 |
| **138** | ABC | 软间隔SVM中引入松弛变量以增强对线性不可分数据和离群噪声的泛化容忍度。 |
| **139** | ABC | SVM在低维直接计算高维内积的三大最经典的标准化核函数。 |
| **140** | ABCD | 感知器算法的目标、准则函数构建、负梯度下降更新法则以及不可分时不收敛的本质缺陷。 |
| **141** | ABC | 最小平方误差判别（MSE）通过伪逆矩阵 $X^{\dagger}$ 实现一步闭式直接求解的工程方案。 |
| **142** | ABD | 多分类SVM的一对多与一对一机制在分类器数、样本使用量及不平衡性上的对比。 |
| **143** | ABC | 惩罚因子 $C$ 对SVM模型复杂度和泛化性的决定性控制作用。 |
| **144** | ABCD | 线性判别超平面的法线方向、位移、代数距离以及对特征空间的切分布置。 |
| **145** | ABCD | 感知器准则函数的关键数学性质（连续、分段线性、凸函数、收敛时值为0）。 |
| **146** | ABCD | 提升学习算法AdaBoost在单轮串行迭代中的完整8步核心骨架流程。 |
| **147** | ABC | 特征选择（挑子集、保原貌）与特征提取（造新特征、失原貌）的根本原理区别。 |
| **148** | ABC | PCA通过协方差矩阵特征值分解实现最大方差保持与最小重构误差的数学内涵。 |
| **149** | ABC | PCA（无监督保持全局最大方差）与LDA（有监督极大化类间/类内比值）的深度对比。 |
| **150** | ABD | 特征选择中启发式搜索策略的典型代表；C项穷举搜索属于全局最优最优搜索。 |

---
*注：本套专项练习题库支持直接打印或导入刷题软件进行期末模拟测试。祝同学们考试顺利，斩获高分！*
